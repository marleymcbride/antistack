# N8N Webhook Configuration
# Update these URLs to match your N8N webhook endpoints

# Primary N8N webhook endpoint (from N8N instructions)
NEXT_PUBLIC_N8N_PRIMARY_WEBHOOK=https://n8n.marleymcbride.co/webhook/3x-energy-leads

# Fallback N8N webhook endpoint
NEXT_PUBLIC_N8N_FALLBACK_WEBHOOK=https://n8n.marleymcbride.co/webhook/systeme-add-contact

# CRITICAL: Update your .env.local and Vercel environment variables to match these URLs
# The primary endpoint was incorrect in the previous configuration!
